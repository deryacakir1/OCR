# -*- coding: utf-8 -*-
"""
Created on Fri Mar 31 03:03:20 2023

@author: Derya
"""

import pytesseract
from pytesseract import Output
import PIL.Image
import cv2


myconfig = r"--psm 6 --oem  3" #tek tip metin blogu oldugundan psm 6 tercih edilir.

text = pytesseract.image_to_string(PIL.Image.open("bookpage.jpg"), config=myconfig)
print(text)


myconfig = r"--psm 6 --oem  3"
img = cv2.imread("bookpage.jpg")
height, width, _= img.shape

boxes = pytesseract.image_to_boxes(img, config=myconfig)

#print(boxes)

for box in boxes.splitlines():
    box = box.split(" ")
    img = cv2.rectangle(img, (int(box[1]), height - int(box[2])), (int(box[3]), height - int(box[4])), (0, 255, 0), 2)
            
cv2.imshow("img", img)
cv2.waitKey(0)


text = pytesseract.image_to_string(img)

with open('Bookpage Output.txt', 'w') as f:
    # Write the extracted text to the file
    f.write(text)

f.close()
